<form method="post" action="{{ path('app_variant_delete', {'id': variant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ variant.id) }}">
    <button class="btn">Delete</button>
</form>
