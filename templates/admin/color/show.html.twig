{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
    <h1>Color</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ color.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_color_index') }}">back to list</a>

    <a href="{{ path('app_color_edit', {'id': color.id}) }}">edit</a>

    {{ include('admin/color/_delete_form.html.twig') }}
{% endblock %}
